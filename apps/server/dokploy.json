{
    "$schema": "../../tooling/scripts/bin/dokploy/schema.json",
    "domain": {
        "production": { "host": "api.${BASE_DOMAIN}" },
        "staging": { "host": "${APP_NAME}-staging.${BASE_DOMAIN}" }
    }
}
